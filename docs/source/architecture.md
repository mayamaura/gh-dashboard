---
title: アーキテクチャ
lede: 何をどこに置き、どのスレッドで動かし、どの経路でデータを取るか。要求仕様 4 章と 9 章を実装単位に落としたもの。
status: ドラフト
version: 0.1
updated: 2026-09-08
---

## 1. 全体像

```
┌────────────────────────────────────────────────────────────────────┐
│ gh-dashboard.exe (常駐 / 非昇格)                                     │
│                                                                     │
│  ┌─────────────────────────┐        ┌──────────────────────────┐  │
│  │ UI プロセス (WebView2)    │        │ コア (Rust / Tauri)        │  │
│  │  React + TypeScript      │        │                            │  │
│  │                          │  IPC   │  commands (IR-01..19)      │  │
│  │  Copilot タブ            │◄──────►│  events   (IR-40..46)      │  │
│  │  Projects タブ           │        │                            │  │
│  │                          │        │  indexer / live / quota    │  │
│  │  appStore (タブ横断)      │        │  scan / git / dev / open   │  │
│  └─────────────────────────┘        └──────────┬───────────────┘  │
│                                                 │                   │
│                        ┌────────────────────────┼───────────────┐  │
│                        │ 読み取り専用            │ 読み書き       │  │
│                        ▼                        ▼               │  │
└────────────────────────┼────────────────────────┼───────────────┼──┘
                         │                        │               │
       ┌─────────────────┴──────────┐   ┌─────────┴──────┐  ┌────▼─────────┐
       │ ~/.copilot/                │   │ app.db (SQLite) │  │ Copilot SDK  │
       │ VS Code workspaceStorage/  │   │ 索引 + 設定のみ  │  │ GitHub REST  │
       │ プロジェクトフォルダ / git   │   └────────────────┘  └──────────────┘
       └────────────────────────────┘
```

境界の意味は 1 つだけ覚えればよい。**左は絶対に書かない (INV-1)。右のうち外向き通信は利用枠の取得だけ (INV-3)。**

## 2. データ取得の 3 系統

要求 4.2 の 3 系統は、実装上も**別のモジュール・別の起動契機・別の永続化方針**として分ける。混ぜた時点で NFR-03 が壊れる。

| 系統 | モジュール | 起動契機 | 触ってよいもの | 永続化 |
|---|---|---|---|---|
| (1) ライブ | `copilot::live` | UI 側の 2 秒タイマー (タブ表示中のみ) | 小さなローカル状態ファイル / PID 生存確認 / トランスクリプト末尾のシーク読み | しない (プロセス内メモリ) |
| (2) インデックス | `copilot::indexer` | タブ表示・更新ボタン・自動 (未取り込みがあるときのみ、下限 10 秒) | セッションログ全体の差分パース / DB | する |
| (3) 利用枠 | `copilot::quota` | ユーザー操作 + 長周期 (5 分以上) | Copilot SDK / GitHub REST / DB (直近値) | 直近値のみ (+ 任意で時系列) |

```
UI (2s タイマー) ──► live_status_get ──► ファイル読み + PID 確認         ── 数 ms
UI (タブ表示)    ──► index_refresh   ──► バックグラウンドタスク ──► DB ──► snapshot イベント
UI (操作/5 分)   ──► quota_get       ──► SDK ──失敗──► REST ──失敗──► 推定 ──► quota イベント
```

<!-- 上の 3 行が守れているかがレビューの第一チェック項目 -->

**プロジェクトタブは定期ポーリングを一切持たない。** タブを開いたときと更新ボタンのときだけスキャンする。

## 3. スレッドとタスク

| 実行文脈 | 何を動かすか | 禁止 |
|---|---|---|
| UI (WebView) | 描画・入力・2 秒タイマー | 重い計算 |
| Tauri メインスレッド | ウィンドウ管理・イベント配送 | **DB / 外部プロセス / ロック / ネットワーク (NFR-20)** |
| 非同期ランタイム (tokio) | IPC コマンドの本体、git 呼び出し、SDK / REST 呼び出し | 長い同期ブロック |
| ブロッキングタスク (`spawn_blocking`) | ディスク走査、差分インデックス、SQLite | |
| dev サーバー読み取りタスク | 子プロセスの stdout / stderr を行読み | |

すべての `#[tauri::command]` は `async fn` とし、同期 IO は `spawn_blocking` に逃がす。**`AppState` のロックを跨いで `.await` しない** (デッドロックの定番)。

## 4. モジュール構成

### 4.1 コア (Rust)

```
src-tauri/src/
├── main.rs              エントリ。ロガー初期化 → lib::run()
├── lib.rs               Tauri Builder。state 構築 / コマンド登録 / 終了フック
├── error.rs             AppError と IPC 境界での変換 (Result<T, String> にしない)
├── state.rs             AppState: DB プール / スキャンキャッシュ / dev レジストリ / 利用枠キャッシュ
├── db/
│   ├── mod.rs           接続を開く。WAL / busy_timeout / foreign_keys
│   └── migrations.rs    バージョン付きマイグレーション (DR-04)
├── util/
│   ├── path_key.rs      パス正規化 (純粋)
│   ├── url_detect.rs    dev サーバー出力からの URL 抽出 (純粋)
│   ├── tail.rs          末尾 N バイトのシーク読みと行分割 (純粋部分を分離)
│   └── relative_time.rs 相対時刻の文言 (純粋)
├── projects/
│   ├── detect.rs        種別判定 (純粋)。マニフェスト有無のリストだけを受け取る
│   ├── scan.rs          1 階層走査 → detect に渡す → スナップショット組み立て
│   ├── git.rs           git 状態の取得 (同時実行上限つき)
│   ├── overrides.rs     手動調整の読み書きとマージ
│   ├── dev_server.rs    dev サーバーの起動 / 停止 / ログリングバッファ
│   ├── external.rs      VS Code / エクスプローラー / ターミナル / Copilot CLI 起動
│   ├── copilot_link.rs  セッション履歴とプロジェクトの紐付け (完全一致 + フォルダ名フォールバック)
│   └── commands.rs      IR-01..06
├── copilot/
│   ├── paths.rs         ~/.copilot / workspaceStorage の場所解決 (COPILOT_HOME 対応)
│   ├── record.rs        1 レコードの DTO と防御的パース (純粋)
│   ├── delta.rs         差分判定と末尾断片の切り捨て (純粋)
│   ├── indexer.rs       差分インデックス本体 (バッチ / トランザクション / 進捗通知)
│   ├── aggregate.rs     セッション集計とサブエージェント行のマージ
│   ├── tree.rs          サブエージェント木の構築 (純粋。孤児・循環をガード)
│   ├── activity.rs      活動状態の合成 (純粋)
│   ├── live.rs          ライブ監視 (IR-10)
│   ├── quota.rs         利用枠の取得と降格 (経路 A→B→C)
│   └── commands.rs      IR-10..19
└── platform/
    ├── mod.rs           OS 差分の薄い入口
    └── win_job.rs       ★ unsafe / FFI はこのファイルだけ (INV-8 / FR-P-68)
```

### 4.2 UI (TypeScript)

```
src/
├── main.tsx             React ルート
├── App.tsx              2 タブのシェル。タブ状態は URL ではなく appStore
├── ipc/
│   ├── commands.ts      ★ invoke を書いてよい唯一の場所 (IR-31)
│   └── events.ts        listen ラッパー。購読解除を必ず返す
├── types/dto.ts         Rust の DTO と 1:1 の型定義
├── store/appStore.ts    タブ横断の共有状態 (FR-C-161 / 164)
├── hooks/
│   ├── useLivePoll.ts   2 秒ポーリング。アンマウント / 最小化で必ず止まる
│   └── useWindowVisible.ts  visibilitychange + window-visibility イベントの合成
├── lib/                 純粋関数 (整形・絞り込み・並べ替え・ゲージの色分け)
├── components/          ゲージ / ガント / 系統図 / セッションカード / プロジェクト行
└── pages/
    ├── CopilotPage.tsx
    └── ProjectsPage.tsx
```

## 5. 主要な流れ

### 5.1 差分インデックス (FR-C-01..15)

```
index_refresh
  └─ 実行中フラグを立てる (立っていたら黙って return)   FR-C-10
     └─ spawn_blocking
        ├─ 対象ファイルを列挙
        ├─ for each file:
        │   ├─ delta::decide(記録済みオフセット, 記録済みサイズ, 現在サイズ)
        │   │     → 継続 / 全再パース / 更新なし                  FR-C-06
        │   ├─ オフセットからストリーミング読み (全体をメモリに載せない) FR-C-08
        │   ├─ 改行で終わらない末尾断片を切り捨てる                FR-C-05
        │   ├─ 3,000 行ごとに:
        │   │     BEGIN
        │   │       turn_index に INSERT OR IGNORE (UNIQUE(file_path, byte_offset)) FR-C-07
        │   │       sessions / subagent_runs / quota_events を更新
        │   │       index_files.last_parsed_offset を更新  ← 必ずトランザクションの最後 FR-C-04
        │   │     COMMIT
        │   │     15ms スリープ (DB ロックを手放す)                FR-C-09
        │   └─ 進捗を 250-300ms に間引いて通知                    FR-C-11
        └─ 完了 → snapshot イベント (IR-44)
```

壊れた行は数えて読み飛ばす。**読み飛ばした件数は UI に出す (NFR-43)。**

### 5.2 ライブ監視 1 周期 (FR-C-40..52)

```
live_status_get (2 秒ごと / タブ表示中のみ)
  ├─ 状態ファイルを列挙 → PID 生存確認 → 死んでいる行を除外   FR-C-40
  ├─ セッションごとに (パス, サイズ, mtime) を前回と比較
  │    同じならディスクを読まない                              FR-C-48
  │    違えば末尾 64KB をシーク読み (見つからなければ 512KB で 1 回だけ) FR-C-47
  │    末尾から遡って最初の本文レコードを採る                    FR-C-49
  ├─ 稼働中サブエージェント "集合" を作る (ログファイルの mtime が 120 秒以内) FR-C-50
  │    件数は集合の長さとして導出する ← 別々に数えない            FR-C-51
  ├─ activity::synthesize(状態シグナル, 末尾レコード, 稼働中サブエージェント集合) FR-C-45
  └─ 一覧から消えたセッションのキャッシュを剪定                  FR-C-48
```

**この経路にネットワークも外部プロセス起動も入れない (INV-4)。**

### 5.3 利用枠の取得と降格 (FR-C-80..95 / 130..144)

```
quota_get(force)
  ├─ 下限間隔 (5 分) 内かつ force でなければキャッシュを返す     FR-C-142
  ├─ 経路 A: SDK account.getCurrentAuth
  │    成功 → source = 実値(sdk)
  │    失敗 → 理由を記録して B へ                               FR-C-136
  ├─ 経路 B: GitHub REST (個人 / 組織の両方を試し、取れた方を使う) FR-C-140
  │    成功 → source = 実値(rest)
  ├─ 経路 C: インデックス済みトークンからの相対推定 (60 秒 TTL)   FR-C-92/93
  │    → source = 推定。UI に必ず「推定」と出す                  FR-C-82
  └─ どれも不可 → source = 取得不可 + 「何をすれば取れるか」      FR-C-83
```

枠は**独立に評価する**。ある枠が実値で取れても、取れなかった別の枠に推定を混ぜない (FR-C-84)。

### 5.4 dev サーバーの生存管理 (FR-P-60..68)

```
アプリ起動時: Job Object を 1 つ作り、KILL_ON_JOB_CLOSE を設定 (platform/win_job.rs)
dev_start:    子プロセスを CREATE_SUSPENDED で起こす → Job に割り当てる → 再開
              stdout/stderr を行読みするタスクを 2 本 spawn
              URL を検出したら dev 状態を更新して projects-dev-status を発火
アプリ終了時: 明示的に全停止 → Job ハンドルを閉じる (取りこぼしを OS 側で回収)
```

ウィンドウを閉じただけ (トレイ常駐継続) では停止しない。**終了イベントでのみ停止する (FR-P-62)。**

## 6. 状態の置き場所

| 状態 | 置き場所 | 理由 |
|---|---|---|
| スキャン対象フォルダ / 手動調整 | SQLite | ユーザーの意思。消えては困る (DR-01) |
| セッション索引・集計 | SQLite | 一覧性のため (DR-03) |
| スキャン結果 / git 状態 / Copilot 利用状況 | プロセス内メモリ | 導出データ。永続化しない (DR-02 / FR-P-04) |
| dev サーバーのログ | メモリ上のリングバッファ 500 行 | DB に入れない (FR-P-64) |
| ライブ状況 | プロセス内メモリ | 2 秒で作り直す |
| インデックス進捗 / 最終取得時刻 | UI 側の appStore | **タブ切替で消えてはいけない (FR-C-161)** |
| タブ別スナップショット | UI 側の appStore | 切替時に即描画するため (FR-C-164) |

## 7. 失敗の扱い

| 失敗 | 方針 |
|---|---|
| 1 プロジェクトの git 取得失敗 | その行だけ「git 情報なし」。全体は成功 (FR-P-43 / NFR-24) |
| 1 セッションのパース失敗 | その行を読み飛ばし、件数を数えて UI に出す (FR-C-12 / NFR-43) |
| 状態ファイルが読めない / 形式が変わった | 該当項目を空にして続行。機能全体を止めない (FR-C-13 / NFR-23) |
| SDK / REST の失敗 | 経路を降格。直前の値と「取得できませんでした」を並べて出す (NFR-07) |
| 未インデックスのセッション詳細 | **エラーではなく `null`。**「まだ記録がありません」と出し、1 回だけインデックスを起動 (FR-C-57 / IR-14) |
| dev サーバーの起動失敗 | 状態を `失敗(理由)` にする。他プロジェクトに波及させない |

## 8. ログ

`%LOCALAPPDATA%\gh-dashboard\logs\` に 5MB × 5 世代でローテーション (NFR-22)。**不具合調査の起点はここに一本化する。**
UI スレッドの応答性 watchdog (3 秒) の記録もここへ (NFR-21)。

**ログにセッション本文・プロンプト・トークン値以外の内容を書かない。** パスは書いてよい (ローカルログのため) が、認証情報は当然書かない。
