---
title: 実装計画
lede: 要求 付録 C の段階を、AI エージェントが 1 件ずつ着手できるタスクに割ったもの。上から順に潰す。段階の完了条件を満たさずに次へ進まない。
status: ドラフト
version: 0.1
updated: 2026-09-08
---

## 進め方

- タスク ID は `T-<段階>.<連番>`。**段階を飛ばさない**
- 1 タスク = 1 コミット を目安にする
- 着手前に、そのタスクの「要求」列の ID を [要求仕様](requirements.html) で読む
- 完了時に [トレーサビリティ](traceability.html) の該当行を埋める
- 状態は `未着手` / `進行中` / `完了` / `保留(OQ待ち)`

> [!注意]
> **段階 0 を飛ばさない。** OQ-02 (バイトオフセット方式が成立するか) と OQ-06 (利用枠が SDK で取れるか) の結論次第で、段階 4 と段階 6 の設計が根本から変わる。推測で先に進めると、書いたコードがまるごと無駄になる。

---

## 段階 0 — 実データ・API 調査

**完了条件**: バイトオフセット方式が成立するかの結論が出ている (OQ-02 = 確定、成立)。**利用枠は経路 A の `account.getCurrentAuth` で「3 枠の率 + 付与額 + 本物の月次リセット日」まで取れることが分かった** (OQ-06 / ADR-0017)。**経路 B は現行スコープで全滅し、履歴用途に降格した** (OQ-12 / ADR-0018)。T-0.8 完了により**段階 6 のブロッカーは無い**。

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-0.1 | `tools/probe/copilot-layout.mjs` — 配下の構成・ファイル数・総容量・最大サイズを出す | OQ-10 | 完了 |
| T-0.2 | `tools/probe/jsonl-shape.mjs` — 1 レコード = 1 行 / 行内改行 / BOM を検査 | **OQ-02** | 完了 |
| T-0.3 | `tools/probe/record-kinds.mjs` — レコード種別ごとの件数・フィールド出現率を集計 | OQ-01 / OQ-07 | 完了 |
| T-0.4 | `tools/probe/live-detect.mjs` — `ide/*.lock` + `logs/process-*.log` で稼働判定できるか | OQ-04 | 完了 |
| T-0.5 | `tools/probe/vscode-sessions.mjs` — `workspaceStorage` から cwd を逆引きできるか | OQ-03 | 完了 |
| T-0.6 | `tools/probe/quota-sdk.mjs` — `account.getQuota` が個人アカウントで返す中身 | **OQ-06** / OQ-08 | 完了 |
| T-0.7 | 結果を [未決事項](open-questions.html) に書き戻し、影響を受ける ADR を更新 | — | 完了 |
| T-0.8 | `tools/probe/quota-rest.mjs` — 経路 B (課金 API) + SDK / REST の同時比較 | **OQ-12** / FR-C-138〜140 | 完了 |

**このタスク群はすべて読み取り専用。** 出力は `tools/probe-out/` (コミットしない)。

---

## 段階 1 — プロジェクト基盤

**完了条件**: 実フォルダで誤検出なく一覧が出る。

**実機確認済み (2026-09-11)**: 実フォルダ 2 か所 (`D:\masah\Projects` / `C:\Users\masah\Documents`) で 16 プロジェクトが一覧に出た。種別は Tauri × 4 / Vite SPA × 3 / その他 × 9 で誤判定なし。本アプリ自身は「起動不可: 本アプリ自身のソースです」。アクセス拒否のジャンクション (`My Music` / `My Videos`) は警告に積まれてスキップされた。

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-1.1 | `util::path_key` — パス正規化 (純粋) + テスト | 用語定義 | 完了 |
| T-1.2 | `projects::detect` — 種別判定 (純粋) + テスト | FR-P-10〜14 | 完了 |
| T-1.3 | DB 初期化とマイグレーション基盤 | DR-01 / DR-04 | 完了 |
| T-1.4 | `project_scan_folders` / `project_overrides` の読み書き (実装: projects/store.rs) | DR-02 / FR-P-30〜32 | 完了 |
| T-1.5 | `projects::scan` — 1 階層走査 → 種別判定 → スナップショット (実装: projects/scan.rs) | FR-P-01〜06 | 完了 |
| T-1.6 | 起動コマンドの解決 (許可リスト順 / 上書き優先) | FR-P-20〜23 | 完了 |
| T-1.7 | IR-01〜03 のコマンド実装 + `projects-snapshot` イベント (実装: projects/commands.rs) | IR-01〜03 / IR-32 | 完了 |
| T-1.8 | 一覧 + 詳細パネルの 2 ペイン UI (実装: components/ProjectRow.tsx / ProjectDetail.tsx / RowMenu.tsx) | FR-P-80 / 84 | 完了 |
| T-1.9 | フロント側の絞り込み・並べ替え (純粋関数 + テスト) | FR-P-81〜82 | 完了 |
| T-1.10 | ヘッダー (最終スキャン / すべて停止 / 更新) とスキャン中表示 (実装: App.tsx / lib/projectsActions.ts / store/appStore.ts) | FR-P-85 / 87 | 完了 |

**気をつける点**

- 既定フォルダを**DB に書き込まない** (FR-P-02)。書くと削除不能になる
- 読めないフォルダはスキップして警告に積む。**スキャン全体を失敗させない** (FR-P-03)
- スキャン結果を**永続化しない** (FR-P-04)
- 走査は `spawn_blocking` (FR-P-06)
- **既定フォルダは `USERPROFILE` から `Documents\Projects` を組み立てる。** `dirs::document_dir()` は OneDrive にリダイレクトされた「ドキュメント」既知フォルダ (`OneDrive\ドキュメント`) を返し、FR-P-02 の `%USERPROFILE%\Documents\Projects` と食い違う (実機で確認)
- **スキャン対象フォルダの保存値はディスク上の大文字小文字** (`std::fs::canonicalize`、`\\?\` 接頭辞は落とす)。重複判定だけ `path_key` で行う。`path_key` (小文字) をそのまま表示に使うと `d:\masah\projects` と出て実物と違って見える
- **1 階層下の探索では `node_modules` / `.git` / `target` / `dist` / `build` / `.venv` / `venv` / `__pycache__` と `.` 始まりを除外する。** `node_modules/<pkg>/vite.config.ts` を拾うと誤判定になる。スキャン対象フォルダ直下の `.` 始まり (`.git` / `.vscode` / `.obsidian` 等) も候補にしないが、**無言では消さず、除外した件数と名前を警告に積む** (FR-P-13 / NFR-43。`.dotfiles` のような本物のプロジェクトを黙って落とさないため)
- **手動調整フォームの初期値は `Project.override_values` (保存済みの上書き値そのもの) から取る。** `display_name` / `resolved_command` / `working_dir` は上書き適用後の表示用の値。保存は行全体の置き換えなので、表示用の値を初期値にすると「表示名だけ直したら `working_dir_override` が消える」が起きる (実装中に検出)

---

## 段階 2 — git + Copilot 利用状況

**完了条件**: フォルダ名フォールバックが実データで期待どおり効く。

**実機確認済み (2026-09-11)**: ユーザーの実 `~/.copilot/session-state` に 49 件のセッションが実在し、うち 6 件が既存プロジェクト (agent-deck) に完全一致で紐付き、43 件が未紐付けとして正しく警告表示された。INV-1 (mtime とサイズが実行前後で不変) を確認済み。フォールバック判定ロジック自体はユニットテスト 15 本で全条件 (3 条件の AND、競合解決の優先順位、決定性) を検証済み。ただし実データでフォルダ名フォールバック分岐 (完全一致が無くリネームされたフォルダ) が実際に発火したケースは今回は観測されなかった (ユーザーの実環境にリネームされたプロジェクトが無かったため) — この限界を明記します。

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-2.1 | `projects::git` — ブランチ / 変更有無 / リモート / 最終コミット | FR-P-40〜47 | 完了 |
| T-2.2 | 同時実行上限 (8) と `CREATE_NO_WINDOW` | FR-P-45 / 46 | 完了 |
| T-2.3 | セッション履歴の部分読み (末尾数 KB / 先頭数十 KB) | FR-P-55 | 完了 |
| T-2.4 | `projects::copilot_link` — 完全一致の紐付け | FR-P-51 | 完了 |
| T-2.5 | フォルダ名フォールバック (3 条件すべて成立時のみ) + テスト | FR-P-52 / 54 | 完了 |
| T-2.6 | UI に「旧パスの履歴 (フォルダ名で照合)」表示 | FR-P-53 / NFR-41 | 完了 |
| T-2.7 | 稼働中判定 (状態ファイル + PID 生存確認) | FR-P-57 | 完了 |

**気をつける点**

- git の判定対象は `working_dir` ではなく**リポジトリルート** (FR-P-44)
- `.git` が無ければ**コマンドを 1 つも実行しない** (FR-P-42)
- **mtime を判定に使わない。中身のタイムスタンプを読む** (FR-P-56)
- 履歴が無いプロジェクトは `null`。**空欄で埋めない** (FR-P-58)
- 完全一致とフォールバックが競合したら**フォールバックを捨てる。合算しない** (FR-P-54)

---

## 段階 3 — dev サーバー

**完了条件**: **アプリを強制終了しても子プロセスが残らない。**

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-3.1 | `platform::win_job` — Job Object (`KILL_ON_JOB_CLOSE`)。**unsafe はここだけ** | FR-P-61 / 68 | 未着手 |
| T-3.2 | dev サーバーの起動 / 停止 / 5 状態管理 | FR-P-60 / 66 | 未着手 |
| T-3.3 | stdout / stderr の非同期行読みと `util::url_detect` | FR-P-63 | 未着手 |
| T-3.4 | リングバッファ 500 行 + 200〜300ms バッファ配信 | FR-P-64 / 65 | 未着手 |
| T-3.5 | アプリ終了イベントで全停止。**ウィンドウを閉じただけでは止めない** | FR-P-62 | 未着手 |
| T-3.6 | 「すべて停止」+ 二段階確認 | FR-P-67 | 未着手 |
| T-3.7 | `projects::external` — VS Code / エクスプローラー / ターミナル / Copilot CLI | FR-P-70〜74 | 未着手 |

**気をつける点**

- 外部ツールは**投げっぱなし。終了コードを見ない** (FR-P-71 / 72)
- ターミナルは Windows Terminal → 失敗したら PowerShell (FR-P-74)
- 起動可否は**バックエンドでも再チェック** (FR-P-23)

---

## 段階 4 — 差分インデックス

**完了条件**: 初回が NFR-01 (5 秒) を満たし、**2 回目が冪等 (新規 0 件)**。

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-4.1 | `copilot::delta` — 差分判定 + 末尾断片の切り捨て (純粋) + テスト | FR-C-03/05/06/15 | 完了 |
| T-4.2 | `copilot::record` — 防御的パース (全フィールド欠落しうる前提) | FR-C-12 / 13 / NFR-23 | 未着手 |
| T-4.3 | 索引テーブルへの書き込み (UNIQUE + `INSERT OR IGNORE`) | FR-C-02 / 07 | 未着手 |
| T-4.4 | バッチコミット (3,000 行) + 15ms スリープ + オフセット更新の順序 | FR-C-04 / 09 | 未着手 |
| T-4.5 | バックグラウンド実行 + 多重起動防止 | FR-C-10 | 未着手 |
| T-4.6 | 進捗通知 (250〜300ms 間引き) | FR-C-11 | 未着手 |
| T-4.2a | `copilot::workspace_meta` — `session-state/<id>/workspace.yaml` のパース (cwd / `client_name` / `name` / `created_at` / `updated_at`)。純粋関数 + テスト | FR-P-51 / FR-C-40 / FR-C-54 / ADR-0003 | 未着手 |
| T-4.7 | セッション集計 (主ソースは session.shutdown.data。session-store.db に依存しない) | FR-C-20 / ADR-0013 | 未着手 |
| T-4.8 | サブエージェント行のマージ (親子両側にあるキー) | FR-C-21〜23 / 29 / ADR-0016 | 未着手 |
| T-4.9 | 完了通知の 2 段階ゲート + 暫定応答を完了扱いしない (OQ-11 待ち) | FR-C-24〜26 | 保留(OQ-11 待ち) |
| T-4.10 | 拒否と失敗の区別 (OQ-11 待ち) | FR-C-27 | 保留(OQ-11 待ち) |
| T-4.11 | 利用枠到達イベントの抽出 | FR-C-28 | 未着手 |
| T-4.12 | `turn_body_get` — 1 レコードのシーク読み (上限 512KB) | IR-15 / FR-C-120 | 未着手 |

**気をつける点**

- ファイル全体をメモリに載せない (FR-C-08)
- **オフセット更新はトランザクションの最後** (FR-C-04)
- 壊れた行は数えて読み飛ばす。件数を UI に返す (FR-C-12 / NFR-43)
- **`workspace.yaml` の `name` は初回プロンプトの本文がそのまま入る。140 字プレビューの上限を掛けて扱う** (INV-6 / ADR-0003)
- **`session.shutdown` は 1 ファイルに複数現れる** (`--resume` のたび)。**終了の確定シグナルにしない** (ADR-0014)
- **`parentId` はレコードの連鎖であってエージェントの木ではない。**サブエージェントの主キーは `toolCallId` (ADR-0016)

---

## 段階 5 — ライブ監視

**完了条件**: **タブ離脱・最小化で確実に止まる。**

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-5.1 | session-state/*/ の列挙 + workspace.yaml 読み + events.jsonl の mtime / 末尾判定 | FR-C-40 / ADR-0014 | 未着手 |
| T-5.2 | 末尾シーク読み (64KB → 512KB 1 回だけ) と本文レコードの遡り探索 | FR-C-47 / 49 | 未着手 |
| T-5.3 | `(パス, サイズ, mtime)` キャッシュと剪定 | FR-C-48 | 未着手 |
| T-5.4 | 稼働中サブエージェント**集合**の算出 (件数は length で導出) | FR-C-50 / 51 | 未着手 |
| T-5.5 | `copilot::activity` — 活動状態の合成 (純粋) + テスト | FR-C-44〜46 | 完了 |
| T-5.6 | `useLivePoll` — 2 秒タイマー。アンマウントで停止 | FR-C-41 / 42 | 未着手 |
| T-5.7 | `window-visibility` イベント + `visibilitychange` の合成 | **FR-C-43** / IR-46 | 未着手 |
| T-5.8 | セッションカード (FR-C-53 の項目一式) | FR-C-53 / 54 | 未着手 |
| T-5.9 | 30 分アイドルの既定非表示 (状態を持たない実装) | FR-C-55 | 未着手 |
| T-5.10 | インライン系統図と自動展開の抑止条件 | FR-C-56 | 未着手 |
| T-5.11 | 未インデックスセッションの穏やかな表示 + 1 回だけ再インデックス | FR-C-57 | 未着手 |
| T-5.12 | 自動インデックスの発火判定 (mtime のみ / 下限 10 秒) | FR-C-58 / 59 / 60 | 未着手 |
| T-5.13 | IDE ワークスペース一覧 (切断済みも残す) (lock は FR-C-70〜72 専用。稼働判定には使わない) | FR-C-70〜72 | 未着手 |
| T-5.14 | 稼働サマリー (3 数字) | FR-C-61 | 未着手 |

**気をつける点**

- **この経路にネットワークも外部プロセス起動も入れない** (INV-4 / NFR-03)
- 自動展開の抑止が無いと「自動展開 → 失敗 → 閉じる → 自動展開」の 2 秒ループになる (FR-C-56)
- 自動インデックスの判定に**固定間隔で更新される値を混ぜない** (FR-C-59)
- **IDE の状態ファイルに `headers` フィールドが存在する。DTO に定義しない** (INV-2 / FR-C-72)
- **`ide/*.lock` の `pid` は VS Code 本体のプロセス ID で、Copilot セッションの生死とは無関係** (実測: lock 側 PID 集合と `logs/process-*.log` 側 PID 集合の交差 0 件)。稼働判定に使わない (ADR-0014)

---

## 段階 6 — 利用枠ゲージ

**完了条件**: 段階降格とラベルが実機で正しく切り替わる。**額と率の両方が出る。**

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-6.1 | `copilot::quota` の型定義 (3 状態 × 枠ごと独立) | FR-C-81 / 84 / ADR-0009 | 完了 |
| T-6.2 | 降格判定 (純粋) + テスト | FR-C-81〜90 | 完了 |
| T-6.3 | 経路 A: SDK `account.getCurrentAuth` (対象はクォータ 3 枠 + `quota_reset_date_utc`: chat / completions / premium_interactions) | FR-C-130 / 131 / ADR-0015 / ADR-0017 | 未着手 |
| T-6.4a | セッション単位の消費 (`totalNanoAiu`)。**ローカルの `events.jsonl` から取る** (SDK 呼び出し不要) | FR-C-132 / ADR-0013 | 未着手 |
| T-6.4b | `models.list` からモデル別単価を取得 | FR-C-134 | 保留(取得不可を確認済み / ADR-0010) |
| T-6.5 | 経路 B: GitHub REST (個人 / 組織の両方を試す) (アカウント全体の月次消費履歴 (FR-C-138/139) と組織 / Enterprise (FR-C-140) 専用。再開条件は user スコープ持ちか組織管理下のアカウントでの実測 / ADR-0018) | FR-C-138〜142 / OQ-12 | 未着手 |
| T-6.6 | 認証は OS 資格情報ストア / GitHub CLI 参照。**保存しない** | FR-C-141 / NFR-31 | 未着手 |
| T-6.7 | 経路 C: ログ由来の推定 (60 秒 TTL) | FR-C-92 / 93 / 143 | 未着手 |
| T-6.8 | ゲージ描画 (自前 SVG)。3 段階色分け + 超過の別建て (クレジット単位のみ。$ 換算を出さない / ADR-0010) | FR-C-88 / 89 / 90 | 未着手 |
| T-6.9 | 鮮度表示 (`observed_at` 基準) と 15 分超の警告色。リセット日判定に `quota_reset_date_utc` を使う (observed_at は自前で保持する。resetDate を使わない / ADR-0015 / ADR-0017) | **FR-C-85** / 86 / 87 | 未着手 |
| T-6.10 | 長周期タイマー (5 分以上、画面表示中のみ) | FR-C-135 / NFR-12 | 未着手 |
| T-6.11 | `quota_source_status_get` — 各経路の可用性表示 | IR-18 / FR-C-83 | 未着手 |
| T-6.12 | `has_quota === false` の枠を「適用外」として描く (率を出さない / 危険色にしない) | FR-C-131 / ADR-0015 | 未着手 |

**気をつける点**

- **2 秒ポーリングに絶対載せない** (FR-C-135)
- 金額表示に**概算である旨を添える** (NFR-45)
- 失敗しても直前の値と「取得できませんでした」を並べる (NFR-07)
- **`remainingPercentage` を再計算しない。** 同じ消費が**丸めの異なる 3 つの値**で返る (`entitlement − quota_remaining` = 1.6 / `entitlement − remaining` = 2 / `credits_used` = 1)。`percent_remaining` は**小数の `quota_remaining` から厳密に出る** (198.4 / 200 = 99.2)
- **適用外の枠は `has_quota === false` で判定する。** `entitlement <= 0` は 0 除算ガードとして別に持つ。**`-1` (無制限) は 1 度も観測していない**
- **`resetDate` の中身はスナップショットの観測時刻** (`quota_snapshots.*.timestamp_utc` と同値。SDK と REST の同時取得でミリ秒一致)。**リセット日は `quota_reset_date` (`2026-10-01`)。`quota_reset_at` は 3 枠とも `0` で使えない**
- **`usedRequests` = `entitlement − remaining` (整数丸め)。** 真の消費 1.55 が 2 になる。**表示にも計算にも使わない**
- **`percent_remaining: 0` をそのままゲージに載せない。** `premium_interactions` が FR-C-88 の危険色 (90% 以上) に該当し、**適用外の枠が真っ赤になる**

---

## 段階 7 — 振り返り UI

**完了条件**: **系統図とガントの階層が一致する。**

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-7.1 | 本日の使用状況 (集計 + 時間帯グラフ + フォルダ別上位 5) | FR-C-100〜103 | 未着手 |
| T-7.2 | 合成モデルの除外と usage 内訳の二重加算防止 | FR-C-104 | 未着手 |
| T-7.3 | モデル別内訳 (トークンとクレジットの両方) | FR-C-105 | 未着手 |
| T-7.4 | セッション検索 (250ms デバウンス / 既定 100・上限 1000) | FR-C-110 / 111 | 未着手 |
| T-7.5 | `copilot::tree` — 木構築 (純粋) + テスト | FR-C-113 / 114 | 完了 |
| T-7.6 | 系統図の描画 (自前 SVG) | FR-C-112 / 113 | 未着手 |
| T-7.7 | ガントチャート + 利用枠到達イベントの縦マーカー | FR-C-118 | 未着手 |
| T-7.8 | ライブ表示の折りたたみ (祖先保護つき) | FR-C-115 / 116 | 未着手 |
| T-7.9 | 振り返り側は全件表示 | FR-C-117 | 未着手 |
| T-7.10 | 本文ビューア (初期 150 件 / 150 件ずつ追加 / クリックでシーク読み) | FR-C-119 / 120 | 未着手 |
| T-7.11 | 系統図・ガント・ビューアの選択とホバー連動 | FR-C-121 | 未着手 |
| T-7.12 | ヘッダー (最終インデックス / 取得経路 / 更新) | FR-C-160 | 未着手 |
| T-7.13 | タブ横断の進行状態と スナップショットキャッシュ | **FR-C-161** / 164 | 未着手 |
| T-7.14 | アニメーション設定 (`自動 / 入 / 切`) と永続化 | FR-C-162 / IR-19 | 未着手 |

---

## 横断タスク

| ID | タスク | 要求 | 状態 |
|---|---|---|---|
| T-X.1 | ログのファイル出力 + ローテーション (5MB × 5 世代) | NFR-22 | 未着手 |
| T-X.2 | UI スレッド応答性の watchdog (3 秒) | NFR-21 | 未着手 |
| T-X.3 | `quota_samples` の間引き (30 日) — **書き込みとセットで** | DR-07 / FR-C-94 | 未着手 |
| T-X.4 | リリースビルドのデバッグシンボル出力とアーカイブ | NFR-25 | 未着手 |
| T-X.5 | トレイ常駐とウィンドウ表示・非表示 | 要求 2.3 / FR-P-62 | 未着手 |
| T-X.6 | 起動時のマイグレーション前バックアップ | DR-04 | 未着手 |

---

## 状態の集計

| 段階 | 完了 / 全体 |
|---|---|
| 0. 調査 | 8 / 8 |
| 1. プロジェクト基盤 | 10 / 10 |
| 2. git + 利用状況 | 7 / 7 |
| 3. dev サーバー | 0 / 7 |
| 4. 差分インデックス | 1 / 13 |
| 5. ライブ監視 | 1 / 14 |
| 6. 利用枠 | 2 / 13 |
| 7. 振り返り UI | 1 / 14 |
| 横断 | 0 / 6 |

「完了」になっているのは、足場として**純粋関数とテストだけ**先に置いたもの。IO を伴う部分はすべて未着手。
