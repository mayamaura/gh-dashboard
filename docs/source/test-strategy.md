---
title: テスト戦略
lede: NFR-50〜54 の実装形。何を純粋関数として固め、何は実機でしか確認できないかを分ける。「テストが通る」で完了としない。
status: ドラフト
version: 0.1
updated: 2026-09-08
---

## 1. 層

| 層 | 対象 | 道具 | 実行 |
|---|---|---|---|
| **A. 純粋関数** | 種別判定 / パス正規化 / 差分判定 / 末尾断片 / 木構築 / URL 抽出 / 活動状態 / 利用枠の降格 | `cargo test` / `vitest` | 常時。**ここが本体** |
| **B. DB** | マイグレーション / 冪等性 / UNIQUE 制約 | `cargo test` (一時ファイル DB) | 常時 |
| **C. 読み取り専用プローブ** | 実データ・実 API への疎通 | `tools/probe/*.mjs` | 手動。**判定はせず数字を出す** |
| **D. 実機確認** | UI の挙動、プロセス終了、最小化での停止 | 手動 (チェックリスト) | 各段階の完了判定 |

> [!注意]
> **NFR-53: 「テストが通る」で完了としない。実際にアプリを起動して挙動を確認してから完了とする。**
> A と B がいくら緑でも、D を通っていない機能は未完成として扱う。

## 2. 層 A — 純粋関数のテスト一覧

NFR-50 が名指ししている 8 つ。それぞれ「壊れ方」が要求に書かれているので、テストケースは**そこから作る**。

### 2.1 種別判定 (`projects::detect`) — FR-P-10〜14

| ケース | 期待 |
|---|---|
| `src-tauri/tauri.conf.json` あり + `package.json` あり | `tauri` |
| `next.config.ts` と `vite.config.ts` の**両方**あり | `nextjs` (**Vite を先に見ると誤判定する。FR-P-11**) |
| `next.config.mjs` / `.cjs` / `.mts` / `.cts` | いずれも `nextjs` |
| `svelte.config.js` あり | `sveltekit` |
| `pyproject.toml` と `requirements.txt` の両方 | `python_package` |
| `*.ipynb` が 1 つ | `notebook` |
| ルートに何もなく `frontend/vite.config.ts` だけ | `vite`、`working_dir = frontend` (FR-P-12) |
| ルートに何もなく `frontend/` と `backend/pyproject.toml` の両方 | 優先順位が上のものを採る。同順位なら**ディレクトリ名昇順** (FR-P-12) |
| どれにも該当しない | `other`。**一覧から隠さない** (FR-P-13) |

判定材料は**マニフェストの存在有無だけ**。中身を読まない。テストはファイル名の配列を渡すだけで書ける形にする。

### 2.2 パス正規化 (`util::path_key`) — 用語定義

| 入力 | 期待 |
|---|---|
| `D:\Projects\Foo` | `d:\projects\foo` |
| `D:/Projects/Foo/` | `d:\projects\foo` |
| `d:\projects\foo\` | `d:\projects\foo` |
| `\\server\share\proj` | UNC を壊さない |
| 空文字 | エラーまたは `None` (パニックしない) |

### 2.3 差分判定 (`copilot::delta`) — FR-C-03/05/06

| 記録済みオフセット | 記録済みサイズ | 現在サイズ | 期待 |
|---|---|---|---|
| 0 | 0 | 1000 | 全パース (0 から) |
| 500 | 500 | 1200 | 継続 (500 から) |
| 500 | 500 | 500 | 更新なし |
| 500 | 800 | 300 | **全再パース** (truncate / 入れ替わり。FR-C-06) |
| 500 | 500 | 499 | **全再パース** |

**mtime を入力に取らないこと自体をテストで固定する** (引数に mtime が無いことがそのまま担保になる)。

### 2.4 末尾断片の切り捨て (`copilot::delta`) — FR-C-05

| 入力バイト列 | 期待する確定分 | 期待するオフセット増分 |
|---|---|---|
| `{"a":1}\n{"b":2}\n` | 2 行 | 全体 |
| `{"a":1}\n{"b":2` | 1 行 | 1 行目まで |
| `{"a":1}` (改行なし) | 0 行 | 0 |
| `\n` のみ | 0 行 (空行) | 1 |
| 末尾が `\r\n` | 行として確定 | 全体 |

### 2.5 木構築 (`copilot::tree`) — FR-C-113/114

| ケース | 期待 |
|---|---|
| 親 → 子 → 孫 | 3 段の木 |
| `parent_agent_id` が NULL | セッション直下 |
| 親が存在しない (孤児) | **ルート直下に置く。捨てない** (FR-C-113) |
| A → B → A の循環 | **無限ループしない**。循環を切ってガードする (FR-C-113) |
| 生データの `depth` が実際の階層と食い違う | **`depth` を無視して親子関係を採る** (FR-C-114) |
| 同じ親に複数の子 | 開始時刻順 |

系統図とガントが同じ入力から同じ行順を返すことをテストで固定する。

### 2.6 URL 抽出 (`util::url_detect`) — FR-P-63

| 入力行 | 期待 |
|---|---|
| `Local:   http://localhost:5173/` | `http://localhost:5173/` |
| `➜  Local:   http://127.0.0.1:3000/` | 取れる (装飾記号に強い) |
| `Running on http://0.0.0.0:8000` | `http://localhost:8000` に読み替える (0.0.0.0 はブラウザで開けない) |
| `Serving at https://localhost:5173` | https を保つ |
| `Compiled successfully` | `None` |
| ANSI エスケープ付き | 除去してから抽出 |

**ポートは自動採番されるため、設定値からは決められない。**出力からしか取れない。

### 2.7 活動状態の合成 (`copilot::activity`) — FR-C-44/45

優先順は ① 状態シグナルが新鮮 (60 秒以内) → ② 末尾レコードのターン終了理由 → ③ 稼働中サブエージェントがあれば上書き。

| 状態シグナル | 末尾レコード | 稼働中サブエージェント | 期待 |
|---|---|---|---|
| 新鮮 = 生成中 | ツール呼び出し | 0 | `generating` (① が最優先) |
| 古い (61 秒前) | ツール呼び出しで停止 | 0 | `tool_running` (② に落ちる) |
| なし | ターン正常終了 | 0 | `waiting_input` |
| なし | ターン正常終了 | 2 | **`subagent_running`** (③ が上書き) |
| なし | 解釈できない | 0 | `unknown` |
| なし | 解釈できない | 1 | **`subagent_running`** |

> [!注意]
> **「長時間のツール実行中」と「ツール許可プロンプトで停止中」を区別する分岐を書かない** (FR-C-46)。両者はログ上まったく同じ形でしか現れない。`tool_running` の 1 値に寄せ、UI で断定しない。

### 2.8 利用枠ビューの降格 (`copilot::quota`) — FR-C-80〜92

| 入力 | 期待 |
|---|---|
| SDK 成功 | `actual(sdk)` |
| SDK 失敗 → REST 成功 | `actual(rest)` |
| 両方失敗 → 推定可 | `estimated` + 「推定」ラベル |
| すべて不可 | `unavailable` + **「何をすれば取れるか」が入っている** (FR-C-83) |
| 枠 A は実値、枠 B は取得不可 | **B に推定を混ぜない** (FR-C-84) |
| `entitlement = -1` | 「無制限」。**率を計算しない。0 除算もマイナス率も出さない** (FR-C-131) |
| `used > entitlement` | `used_pct > 100` を許す。**100 でクランプしない**。超過額を別建て (FR-C-90) |
| `reset_at` が現在より過去 | **その観測を除外する** (FR-C-85) |
| `observed_at` が 16 分前 | 警告色 (FR-C-87) |
| `used_pct` = 70 / 89 / 90 | 警告 / 警告 / 危険 (FR-C-88) |

**`received_at` を鮮度判定に使ったら落ちるテスト**を必ず 1 本入れる (FR-C-86)。

### 2.9 タイムスタンプパース (`util::time::parse_iso8601_ms`) — FR-P-55 / ADR-0021

| 入力 | 期待 |
|---|---|
| `2026-09-07T17:10:51.671Z` | 正常パース (epoch ms) |
| `2026-09-08T04:58:34.381-07:00` | 正常パース (オフセット付き。REST API の形式) |
| `2026-09-08T11:58:34.381Z` | 正常パース (3 つの API が同じ時刻を返すテスト) |
| `invalid` / 空文字 | `None` (エラーではなく、**パース不可は `None`**) |
| ナノ秒精度以上 | `None` (ミリ秒止まりを強制) |

**`serde_yaml` / `chrono` を入れない。** 固定書式のみ対応する自前パーサ (`src-tauri/src/util/time.rs::parse_iso8601_ms`)。

### 2.10 末尾読み分割 (`copilot::delta::drop_leading_partial`) — FR-C-05

| 入力バイト列 | 期待する確定分 | 期待するオフセット増分 |
|---|---|---|
| `{"a":1}\n{"b":2}\n` | 2 行 | 全体 |
| `{"a":1}\n{"b":2` | 1 行 | 1 行目まで |
| `{"a":1}` (改行なし) | 0 行 | 0 |
| `\n` のみ | 0 行 (空行) | 1 |
| 末尾が `\r\n` | 行として確定 | 全体 |

### 2.11 稼働中判定 (`copilot::activity::is_session_active`) — FR-P-57 / ADR-0014 / ADR-0020

| 観測時刻との距離 | 末尾レコード | 稼働中サブエージェント | 期待 |
|---|---|---|---|
| 直近 (60秒以内) | 生成中シグナル | 0 | `true` |
| 61秒前 | 生成中シグナル | 0 | `false` (シグナルが古い) |
| 60秒以内の閾値 (FR-P-57 の `ACTIVE_MTIME_WINDOW_MS = 120000` = 120秒を実装済み) | — | — | **ADR-0020 で、段階5と共有する同一閾値** |
| — | ツール実行中レコード | 1 | `true` (サブエージェント優先度あり) |
| — | ターン終了 | 0 | `false` |
| 測定不可 (mtime がない) | — | — | `false` |
| — | 解釈不可レコード | 0 | `false` (conservative) |

### 2.12 workspace.yaml と ISO8601 パース (`copilot::parser::parse_workspace_yaml` / `preview`) — FR-P-51 / ADR-0021

| 入力 YAML | 期待 |
|---|---|
| 標準形式 (実測 375 バイト) | `id` / `cwd` / `client_name` / `name` (初回プロンプト) / `created_at` / `updated_at` を取得 |
| `name` に改行・長文 | 140 字プレビューの上限を掛けて扱う (INV-6) |
| ファイル不在 | `None` (エラーではなく正常系) |
| JSON / TOML など別形式 | `None` (YAML のみ対応) |
| ブロックスカラー / 複雑な構造 | 未観測。実データで見つかったら再検討 |
| `cwd` がパス形式でない | エラーではなく、値をそのまま返す (バリデーションは呼び側) |

### 2.13 末尾シーク読み (`copilot::parser::scan_tail`) — FR-P-55 / FR-C-47

| 入力ファイル | 読み込みサイズ | 期待 |
|---|---|---|
| 64 KB 以下 | 全読み | 全レコード |
| 64 KB 以上で末尾 512 KB 以内 | 末尾 64 KB だけ読み | 末尾の行が確定しているなら確定分を返す |
| 末尾断片あり | — | 断片を除いた確定分のみ |
| パース失敗行を含む | — | エラーにならず、読める行だけを返す |
| 空ファイル | — | 0 行 |

### 2.14 Copilot 紐付け — フォルダ名フォールバック (`projects::copilot_link`) — FR-P-51/52/54

**最重要。3 条件の AND、競合解決の優先順位、決定性を ユニットテスト 15 本で検証済み。**

| ケース | 期待 |
|---|---|
| **完全一致**: session の cwd + path_key がプロジェクトと一致 | プロジェクト ID に紐付く。スコア最高 |
| **フォルダ名フォールバック (3 条件全て必須)**: (1) 完全一致が無い、(2) フォルダ名が一意、(3) git リポジトリ | プロジェクト ID に紐付く。スコア中 |
| 完全一致とフォールバック競合 | 完全一致を採る。フォールバックを捨てる (競合解決) |
| フォールバック候補が複数 | `None` (フォルダ名が一意でない) |
| 紐付けられないセッション | 警告に積み、一覧から隠さない (NFR-43) |
| path_key の大文字小文字差 | 無視してマッチ (パス正規化済み) |
| 段階1 先行実装の matchedByLabel と整合 | UI に「旧パスの履歴 (フォルダ名で照合)」表示を正しく出す |

## 3. 層 B — DB のテスト

| ケース | 期待 |
|---|---|
| 空 DB にマイグレーション適用 | 全テーブルができる。`user_version` が上がる |
| 同じマイグレーションを 2 回適用 | 冪等。壊れない |
| 同じ `(file_path, byte_offset)` を 2 回挿入 | 2 件目は無視される (FR-C-07) |
| インデックスを 2 回連続実行 | **2 回目の新規レコードが 0 件** (NFR-02) |
| 途中でトランザクションを中断 | オフセットが進んでいない = 取りこぼしなく再開できる (FR-C-04) |
| `quota_samples` の間引き | 30 日より古い行が消え、新しい行は残る (DR-07) |

## 4. 層 C — 読み取り専用プローブ

実データと実 API に対して**数字を出すだけ**のスクリプト。合否判定をせず、OQ を潰す材料を作る (NFR-52)。

実行方法は共通で:

```
node tools/probe/<名前>.mjs          人が読む形
node tools/probe/<名前>.mjs --json   JSON だけ
COPILOT_HOME=<path> node tools/probe/<名前>.mjs   ~/.copilot の場所を上書き
```

| スクリプト | 調べるもの | OQ |
|---|---|---|
| `copilot-layout.mjs` | 構成・ファイル数・総容量・最大サイズ | OQ-10 |
| `jsonl-shape.mjs` | 1 レコード 1 行 / BOM / 改行コード / 行長分布 / **バイトオフセット往復検証** | OQ-02 |
| `record-kinds.mjs` | レコード種別ごとの件数・バイト数、フィールド出現率、サブエージェント候補キー | OQ-01 / OQ-07 |
| `live-detect.mjs` | `ide/*.lock` と `logs/process-*.log` の PID 生存・対応 | OQ-04 |
| `vscode-sessions.mjs` | `workspaceStorage` のハッシュ再計算と `chatSessions` の中身 | OQ-03 |
| `quota-sdk.mjs` | `account.getQuota` / `getCurrentAuth` / `models.list` の戻り値 | OQ-06 / OQ-08 |
| `quota-rest.mjs` | 公開課金 API 10 件の status、`copilot_internal/user` の中身、SDK との突き合わせ | OQ-12 / OQ-06 |

`quota-sdk.mjs` だけは Copilot SDK が要ります。**リポジトリの `package.json` には足しません** (NFR-10)。準備はこう書いてください:

```
mkdir -p tools/probe-sdk && cd tools/probe-sdk
npm init -y && npm install @github/copilot-sdk
```

`COPILOT_SDK_PATH` で別の場所を指すこともできます。SDK が無い場合は「SDK 未導入」と出して正常終了します。

> [!注意]
> プローブは**読み取り専用**。書き込み・設定変更を一切行わない (INV-1)。
> 出力は `tools/probe-out/` (`.gitignore` 済み)。**個人のセッション内容を含みうるのでコミットしない**。
> **実データを作るときは `COPILOT_HOME` を使い捨てディレクトリに向ける。**ユーザーの実 `~/.copilot` を測定のために汚さない (INV-1)

## 5. 層 D — 実機確認チェックリスト

各段階の完了条件。**ここを通らないものは未完成。**

> **確認のしかた**: `npm run app:dev` で起動し、ウィンドウを撮って目で見る。この環境は 125% DPI なので、`MoveWindow` / `GetWindowRect` を使う補助スクリプトは `SetProcessDPIAware()` を先に呼ぶこと (呼ばないと座標が 1/1.25 に仮想化され、ウィンドウの左上一部しか撮れない)。「テストが通った」を実機確認の代わりにしない (NFR-53)。

### 段階 1〜2 (プロジェクト基盤 / git・利用状況)

- [x] 実フォルダで一覧が出る。種別の誤判定がない (2026-09-11: 16 件、Tauri × 4 / Vite SPA × 3 / その他 × 9)
- [ ] Next.js + Vitest 構成が `nextjs` と判定される (実フォルダに該当構成が無い。`scan.rs` のフィクスチャテストで担保)
- [ ] `.git` の無いフォルダで git コマンドが 1 つも実行されない
- [ ] フォルダをリネームしても、フォルダ名フォールバックで履歴が紐付く
- [ ] フォールバックで紐付いた行に「旧パスの履歴」表示が出る (FR-P-53)
- [ ] 履歴が無いプロジェクトが 0 埋めされていない
- [x] 既定フォルダが未登録のとき注記が出て、DB に書かれない (FR-P-02)
- [x] スキャン対象フォルダの追加・削除が UI から効き、実際の大文字小文字で表示される (IR-03)
- [x] アクセス拒否のフォルダが警告に積まれ、スキャンが失敗しない (FR-P-03)
- [x] 本アプリ自身が「起動不可: 本アプリ自身のソースです」になる (FR-P-22)
- [x] 存在しない作業ディレクトリ上書きが拒否され、DB に書かれない (FR-P-32)
- [x] 手動調整の保存で「最終スキャン」が動かない = 再スキャンしていない (FR-P-05)
- [x] 行内メニューが右端で左に開き、画面外に出ない (FR-P-88)

### 段階 3 (dev サーバー)

- [ ] 起動 → URL が自動検出されて表示される
- [ ] **タスクマネージャーからアプリを強制終了 → 子プロセスが 1 つも残らない** (FR-P-61)
- [ ] ウィンドウを閉じてトレイ常駐 → dev サーバーは**止まらない** (FR-P-62)
- [ ] アプリ終了 → 全 dev サーバーが止まる
- [ ] 停止済みのものに再度停止 → エラーにならない (FR-P-66)
- [ ] `explorer.exe` で開く → 成功扱いになる (終了コード 1 でも。FR-P-72)

### 段階 4 (差分インデックス)

- [ ] 初回が実データで 5 秒以内に完走 (NFR-01)
- [ ] 2 回目が 1 秒未満、**新規レコード 0 件** (NFR-02)
- [ ] 実行中に CPU が 1 コア換算 15% を超えない (NFR-04)
- [ ] インデックス中にファイルが追記されても壊れない
- [ ] 壊れた行があっても止まらず、件数が UI に出る

### 段階 5 (ライブ監視)

- [ ] **タブを離れるとポーリングが止まる** (ログで確認)
- [ ] **ウィンドウを最小化するとポーリングが止まる** (FR-C-43。`visibilitychange` だけでは止まらないことも確認)
- [ ] 死んだセッションが一覧から消える
- [ ] アイドルなセッションでディスクが読み直されない (FR-C-48)
- [ ] サブエージェントのバッジ件数と系統図の行数が一致する (FR-C-51)
- [ ] 未インデックスのセッションで赤いエラーが出ない (FR-C-57)

### 段階 6 (利用枠)

- [ ] 実値 / 推定 / 取得不可 のラベルが実機で正しく切り替わる
- [ ] 額と率の両方が出る (`$6.20 / $10.00 (62%)`)
- [ ] 超過時に 100% でクランプされず、超過額が別建てで出る
- [ ] ネットワークを切っても画面操作がブロックされない (NFR-07)
- [ ] 取得中も他のタブが操作できる

### 段階 7 (振り返り UI)

- [ ] 系統図とガントの階層が一致する (FR-C-114)
- [ ] 本文ビューアが 1 レコードだけを読む (大きなセッションでも即座に開く)
- [ ] タブを切り替えてもインデックス進捗が消えない (FR-C-161)

## 6. 入力操作の検証

ドラッグ等の検証に**合成イベント (`dispatchEvent`) を使わない** (NFR-54)。合成イベントはハンドラの配線しか検証できず、実際の不具合を素通りさせる。実入力相当のイベント注入を使う。

## 7. 実データ由来のテストケース

**実データで見つかった例外パターンは、そのままテストケースにする (NFR-51)。**
見つけたら以下に追記していくこと。

| 発見日 | 現象 | テスト |
|---|---|---|
| 2026-09-08 | `~/.copilot/ide/*.lock` に `headers` フィールドが存在する (認証情報を含みうる) | DTO に `headers` が無いことをコンパイル時に担保 (INV-2) |
| 2026-09-08 | `~/.copilot/` に `session-state/` が存在しないことがある (CLI 未使用の環境) | ディレクトリ不在で 0 件を正常系として返すテスト |
| 2026-09-08 | `events.jsonl` の 1 レコードが 36,428 バイトある (`system.message`)。1 往復の些細なセッションでも 45,354 バイト | 大きい行でオフセット往復が壊れないテスト。行長 p95 17,361 / max 36,428 をケースに使う |
| 2026-09-08 | `session.shutdown` が 1 ファイルに 4 件現れる (`--resume` のたびに増える) | 「shutdown を見たら終了」で判定しないテスト。複数 shutdown を含む JSONL で最終活動時刻が正しく出ること |
| 2026-09-08 | `--resume` は既存部分を書き換えず追記のみ (先頭 133,394 バイトの SHA-256 が一致) | 差分判定がサイズ増加だけで成立することのテスト |
| 2026-09-08 | `session.resume` の `parentId` が直前の `session.shutdown` を指す | 再開をまたいでレコード連鎖が切れないテスト |
| 2026-09-08 | `parentId` が `null` になるのは `session.start` のみ (77 件中 3 件) | `parentId: null` を欠損として扱わないテスト |
| 2026-09-08 | `agentId` はサブエージェント所属レコードにのみ付く (77 件中 18 件 = 23.4%) | `agentId` 不在を「親エージェント」と解釈するテスト |
| 2026-09-08 | JSON 文字列値の中に改行文字を含む行が 22 件ある (エスケープ済み。行分割は壊れない) | エスケープ済み改行を含む行で行数 = レコード数が保たれるテスト |
| 2026-09-08 | `premium_interactions` は `entitlement: 0` **かつ `has_quota: false` かつ `percent_remaining: 0`** | `-1` (無制限) と `0` の両方で 0 除算しないテスト。**`has_quota: false` の枠が危険色にならないテスト** |
| 2026-09-08 | `remainingPercentage` が `(entitlement − used)/entitlement` と一致しない (99.2% vs 99.0%) | 率を再計算せず API の値を使うテスト |
| 2026-09-08 | `resetDate` の中身は `quota_snapshots.*.timestamp_utc` (SDK と REST の同時取得でミリ秒一致)。同一プロセス 3 回連続で不変。**値が変わっていないのに動いた実例あり** (`quota_remaining` 198.4 のまま 3 時点で変化) | `resetDate` をリセット日にも鮮度の変化検知にも使わないテスト。`quota_reset_date_utc` で FR-C-85 の除外を判定するテスト |
| 2026-09-08 | `AccountQuotaSnapshot` の型宣言に無い `hasQuota` / `tokenBasedBilling` が実際には返る | 未知フィールドがあってもパースが落ちないテスト |
| 2026-09-08 | `models.list({})` が `"auto"` 1 件だけを返し `billing` フィールドが無い | 単価が取れないとき金額換算を出さないテスト (ADR-0010) |
| 2026-09-08 | VS Code の `workspaceStorage/<hash>` を再計算で照合できたのは 20 件中 6 件のみ | ハッシュ照合の失敗を正常系として扱い、フォルダ名フォールバックへ落ちるテスト (FR-P-52) |
| 2026-09-08 | VS Code の `chatSessions/*.jsonl` は `kind:0` スナップショット + `kind:1`/`kind:2` パッチ行の形式。cwd は `kind:0` だけでは 0 件、全行走査で 9/26 ファイル | 先頭部分読みだけでは cwd が取れない前提のテスト (FR-P-55) |
| 2026-09-08 | VS Code セッションの `creationDate` は epoch ms。**終了時刻フィールドが無い** | 終了時刻を `null` として扱い空欄で埋めないテスト (FR-P-58) |
| 2026-09-08 | `workspaceStorage` の孤児化 (folder が実在しない) が 20 件中 2 件 | 孤児セッションを警告に積んで一覧から落とさないテスト |
| 2026-09-08 | `~/.copilot/ide/*.lock` の `pid` は VS Code 本体のもので、`logs/process-*.log` の PID 集合との交差が 0 件 | lock の PID を稼働セッション判定に使わないテスト (ADR-0014) |
| 2026-09-08 | `session-store.db` の `sessions.repository` / `host_type` / `branch` が `null` になる (git 管理外の cwd) | これらが `null` でも紐付けが落ちないテスト |
| 2026-09-08 | `sessions.updated_at` は最後のイベントより 4〜14 秒早い (ターン開始時に書かれる) | 最終活動時刻に `updated_at` を使わないテスト |
| 2026-09-08 | 同じ消費量が 3 通りの丸めで返る (`entitlement − quota_remaining` = 1.6 / `entitlement − remaining` = 2 / `credits_used` = 1) | 率と消費量が同じ元データから出ることを確かめるテスト |
| 2026-09-08 | `quota_reset_at` が 3 枠とも `0` (本物のリセット日は別フィールドの `quota_reset_date`) | `quota_reset_at` をリセット日に使わないテスト |
| 2026-09-08 | `entitlement` に `-1` (無制限) は**未観測**。`unlimited: true` も**未観測** | 無制限分岐は**未観測であることをテスト名に書く** (例: `未観測: entitlement が -1 のとき率を出さない`) |
| 2026-09-11 | `dirs::document_dir()` が OneDrive リダイレクト先 (`C:\Users\masah\OneDrive\ドキュメント`) を返し、FR-P-02 の `%USERPROFILE%\Documents` と食い違う | 既定フォルダを `USERPROFILE` から組み立てるテスト (既知フォルダ API に依存しない) |
| 2026-09-11 | `Documents\My Music` / `My Videos` はアクセス拒否 (os error 5) のジャンクション | 読めないサブフォルダが警告に積まれ、スキャン全体が失敗しないテスト (FR-P-03) |
| 2026-09-11 | `std::fs::canonicalize` が `\\?\D:\...` の接頭辞付きで返す | 表示用パスから `\\?\` を落とすテスト |
| 2026-09-11 | 小文字化した `path_key` を表示に使うと `d:\masah\projects` と出る | 保存値がディスク上の大文字小文字を保ち、重複判定は `path_key` で行うテスト (`store.rs::add_keeps_disk_casing_and_dedupes_by_key`) |
| 2026-09-11 | `node_modules/<pkg>/vite.config.ts` が 1 階層下の探索で拾われうる | 除外リストが効くテスト (`scan.rs`) |
| 2026-09-11 | 本アプリ自身 (gh-dashboard) が Tauri + `npm run dev` として普通に検出される | `CARGO_MANIFEST_DIR` の親と `path_key` が一致したら起動不可にするテスト (`scan.rs::is_self_source`) |
| 2026-09-11 | `working_dir_override` を `package.json` の無いサブフォルダにすると候補が空になる | 上書き後の作業ディレクトリで `scripts` を読み直し、無ければ起動不可に落ちるテスト |
| 2026-09-11 | 手動調整の保存は行全体の置き換えなので、表示用の値をフォーム初期値にすると他の上書きが黙って消える | `override_values` からフォームを初期化し、保存後に同じ値が戻ることを確かめる (実機で確認。純粋関数ではないのでユニットテストは無し) |
| 2026-09-11 | 行内メニューを右端の ⋮ から右に開くとウィンドウ外に出る | 画面端で反転する位置補正 (`RowMenu.tsx`、実機で確認) |
| 2026-09-11 | `.` 始まりのトップレベルフォルダを無条件に除外すると、`.dotfiles` のような本物のプロジェクトが警告無しに消える | 除外した件数と名前が警告に積まれるテスト (`scan.rs::dot_dirs_are_excluded_but_reported`) |
